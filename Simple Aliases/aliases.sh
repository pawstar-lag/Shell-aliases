# Basic environment variables are already defined.
alias path = "echo $PATH"
alias pi = "echo 'scale=1000; 4*a(1)'|bc -l"
alias sl = "ls -aLf"
alias ali = "alias"
alias ld = "ls -a"
# Git needs to be installed.
alias push = "git push"
alias pull = "git pull"
alias addall = "git add ."
alias expomarker = "export"
